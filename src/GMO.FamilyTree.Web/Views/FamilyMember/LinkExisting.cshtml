@model GMO.FamilyTree.Web.Models.LinkExistingViewModel
@{
    ViewData["Title"] = Model.ActionLabel;
}
<h1 class="ft-page-title">@Model.ActionLabel</h1>
<p class="ft-page-lead">Link an existing person as @Model.ActionLabel.ToLower() of <strong>@Model.ContextMemberName</strong>.</p>

@if (Model.Candidates.Count == 0)
{
    <p class="text-muted">No one else in this tree can be linked.</p>
    <a asp-controller="Home" asp-action="Index" class="btn btn-outline-secondary">Back to tree</a>
}
else
{
    <form asp-action="LinkExisting" method="post">
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
        <input type="hidden" asp-for="ContextMemberId" />
        <input type="hidden" asp-for="FamilyTreeId" />
        <input type="hidden" asp-for="RelationshipType" />
        <input type="hidden" asp-for="IsChild" />
        <input type="hidden" asp-for="ContextMemberName" />
        <input type="hidden" asp-for="ActionLabel" />
        <div class="mb-3">
            <label class="form-label">Select person to link</label>
            <select asp-for="ExistingMemberId" class="form-select">
                <option value="">-- Choose --</option>
                @foreach (var c in Model.Candidates)
                {
                    <option value="@c.Id">@c.DisplayName</option>
                }
            </select>
            <span asp-validation-for="ExistingMemberId" class="text-danger"></span>
        </div>
        <div class="ft-form-actions">
            <button type="submit" class="btn btn-primary">Link</button>
            <a asp-controller="Home" asp-action="Index" class="ft-link">Cancel</a>
        </div>
    </form>
}
@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
